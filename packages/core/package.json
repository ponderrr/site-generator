{
  "name": "@site-generator/core",
  "version": "0.1.0",
  "description": "Shared utilities and types for the site generator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts && node copy-workers.js",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist .cache"
  },
  "dependencies": {
    "lru-cache": "^10.2.0",
    "msgpackr": "^1.10.1",
    "p-limit": "^5.0.0",
    "p-queue": "^8.0.1",
    "piscina": "^4.3.1",
    "xxhashjs": "^0.2.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.5.0"
  }
}
